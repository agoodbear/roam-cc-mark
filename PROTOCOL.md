# CC 讀取／回寫協定（請CC修改）v2

> 定位：這是 Bear 的「紅筆批註」工具。**紙是 Bear 的，紅圈是 Bear 畫的；CC 只能在圈裡寫鉛筆字（提案），Bear 點頭才變墨水。**
> CC（Claude Code，透過 Roam MCP 從外部）改稿前**先讀完這份**。原稿範圍 = **一個 Roam page，一頁一篇**。

## 一、憲法（凌駕一切流程，違反即中止）

1. **CC 不得直接 `update_block` 任何原稿 block。**（原稿＝Bear 的字）
2. **CC 不得刪除 Bear 建的標記 block。**（清除只由 Bear 的 ✅ 觸發，透過 extension）
3. **CC 不得在原稿區新增 block。**
4. CC 的所有產出，只能寫進「**標記 block 本身（提案欄）**」——換句話說，CC 的字永遠關在標記裡、帶著身分，不匿名混入正文。
5. Bear 是唯一有權讓字進入原稿的人。

## 二、標記格式

Bear 建立（待處理）：
```
#請cc修改 【指令】<意圖>：<一句話> 【第N處】 【原文】「<精確片段>」
```
- 意圖 ∈ {潤, 接, 查, 議}（見四）。`【第N處】` 只在原文重複時出現，指第幾個出現。無【原文】= 整段。

**跨 block 標記（2026-09-08 新增，v13 調整掛載位置）**：Bear 框選連續多個 block 時（Roam 會轉成藍底 block 多選），
extension 建的是**一個標記**、掛在**最後一段**底下，並多一個欄位：

```
#請cc修改 【指令】潤：<指令> 【範圍】共 N 段（由上而下）：<uid1> <uid2> …
```

- **為什麼掛最後一段**：掛第一段的話，標記 block 會夾在 A 和 B 中間，把「本來就該讀成一體」的兩段
  從視覺上切開——那正好違背 Bear 框選它們的理由。掛最後一段，標記落在整個範圍之後。
- `【範圍】` 會**列出全部 uid（含標記所掛的那段），由上而下**，所以 `rangeUids[0]` 就是第一段。
- 畫面上**範圍內每一段都會上底線**，一眼看得出涵蓋到哪裡。

- **為什麼是一個標記不是 N 個**：Bear 會框選 A、B 是因為**這兩段在講同一件事**。拆成兩個獨立標記，
  CC 在任何一邊都看不到全貌（掛在 A 的指令說「合併」，卻不知道要跟誰合併）。
- **CC 收到【範圍】要做的事**：把範圍內**所有** block 的內容一起讀進來再想提案，不要只看 parent 那一段。
- **提案有兩種寫法，決定套用行為**：
  - **合併**（把 N 段併成一段）→ 提案就寫**一段**文字。套用時第一段換成提案，其餘段被改寫成
    `🗑 已併入上一段（確認後請自行刪除）｜原文：…`，**程式不刪任何 block**（可能有 children），由 Bear 自己清。
  - **各段各自改**（例如「這幾段語氣統一一下」）→ 提案寫 N 段，**段與段之間用單獨一行 `---` 分隔**。
    段數對得上就一段對一段替換；對不上會退回「合併」那條路，所以分隔線數量要數清楚。
- `【範圍】` 在欄位順序中排在 `【指令】` 之後、`【第N處】`／`【原文】` 之前。
- ⚠️ **欄位內文（尤其【備註】）不得再出現「【提案】」「【備註】」「【原文】」「【指令】」這幾個字樣**：extension 用這些字樣切欄位，備註裡寫到「需要【提案】…」會被當成提案套進原稿（2026-09-03 實際發生：要刪的一句被換成備註殘句）。要提到欄位名，寫「提案欄」「備註欄」。

CC 回寫（待審）＝把 tag 從 `#請cc修改` 換成 `#cc提案`，並補欄位，**欄位順序固定**：
```
#cc提案 【指令】…【第N處】【原文】「…」【提案】「<替換/草稿文>」【備註】<一句說明/來源>
```

## 三、一次讀全（單一 MCP call，別逐個撈）

待處理（Bear 剛丟給你的）：
```clojure
[:find ?childUid ?markStr ?parentUid ?parentStr
 :where
   [?t :node/title "請cc修改"]
   [?c :block/refs ?t] [?c :block/uid ?childUid] [?c :block/string ?markStr]
   [?p :block/children ?c] [?p :block/uid ?parentUid] [?p :block/string ?parentStr]
   [?p :block/page ?pg] [?pg :node/title "<頁名>"]]
```
把 `"請cc修改"` 換成 `"cc提案"` 即撈「已提案待審」的。Bear 也可在 extension 右下點 📋 打包成一段文字（含 page uid）貼給你，你就零查詢開工。

## 四、協作意圖（四種，各有明確回寫形態）

| 意圖 | Bear 要的 | CC 做什麼 | 回寫 | 守住 Bear 的聲音 |
|---|---|---|---|---|
| **潤** | 這句照我意思改好（口語化/縮短/去AI腔/台灣用語都算） | **最小編輯距離**改被圈的字 | `【提案】「替換文」`；tag→cc提案 | 只動被圈的字，保留 Bear 用詞、句式、判斷語氣；**禁擴寫禁加料、禁加 AI 過場語**；改幅超出指令寧可退回問 |
| **接** | 我卡住了，幫我起一段草稿 | 讀整頁上下文 + 風格檔 `reference_ecg_writing_style_profile` 寫草稿 | `【提案】「草稿」`；tag→cc提案 | 這是素材不是成品；被接受後會自動掛 `#cc草稿`＝還不是 Bear 的字，等 Bear 改寫收編 |
| **查** | 這數字/引用/說法可靠嗎、補來源 | 查證（**醫學數字照鐵律：兩獨立路徑交叉、走 OpenEvidence**） | `【備註】結果＋來源` **＋【提案】把來源自然織進原句的整合版**（＝原文＋citation，最小編輯、不重寫整句、不改語氣）；tag→cc提案 | 提案只把來源織進 Bear 原句；**查不實／不宜改字時不給【提案】**、只寫【備註】「待確認」，禁編造 |
| **議** | 給我 2–3 個說法選項／順序建議 | 給選項或結構建議 | `【備註】編號清單` **＋（若有明確首選）【提案】首選的改寫版**；tag→cc提案 | 提案＝你建議的首選寫法供一鍵套用；Bear 仍可「完成不改／退回」 |

**標記掛在標題上時（2026-08-28 起）**：extension 會在你標記的當下問一句「要改的是底下的內容還是標題本身」，選前者就自動把標記改掛到該節第一段。若仍掛在標題上，「接」的草稿會插到**該節最後**而不是標題的兄弟位置（插在兄弟位置會掉到整節外面）。CC 這邊要記得：**「接」永遠是插入、不會刪任何原稿**，提案文裡寫「請把原本那段刪掉」只是給人看的，程式不會執行——要取代某段就用「潤」並把標記掛在那一段上。

回寫形態：**替換型**（潤，及查/議的整合版）、**插入型**（接）、**註記型**（查/議的【備註】）。**查/議＝【備註】(意見)＋【提案】(整合版) 並存**：Bear 在卡片上可按「✅ 套用整合版」(替換原句) 或「完成·不改」(只清標記)。整合版寫不出或不該寫就只留【備註】、不給【提案】。

## 五、對帳清單（回寫完，在 chat 回一份，Bear 一眼知道回 Roam 要看什麼）

一處一行：
```
#3 潤｜「到院前已去顫三次」→ 建議「到院前電擊了三次」（12字→10字）
#5 查｜D2B 90 分鐘 → 已證實，來源：2013 ACCF/AHA STEMI Guideline
#7 接｜（第 4 段結尾）已補一段 82 字草稿，接受後請改寫收編
```

## 六、生命週期與收斂

```
#請cc修改（Bear 建，待處理）
   → CC 回寫 → #cc提案（待審，含【提案】/【備註】）
      → Bear ✅ 接受 → extension 套用（潤=替換原文第N處／接=插入新 block 掛 #cc草稿／查議=「套用整合版」替換原句 或「完成·不改」只刪標記）
      → Bear ↩ 退回 → 改指令 → tag 換回 #請cc修改 → 再送（面板會顯示你上一版提案當參考）
      → Bear ✎ 改進這句（查/議專用）→ 轉成潤稿請求，指令已帶上你查到的來源/建議
         （例：潤：把查證到的來源整合進這句：<來源>）→ 再送 → CC 提「原文＋來源」整合好的句子
（標記消失＝這處收斂）  全頁標記＝0 ＝ 定稿

旁支（整篇重排版，見九）：
#cc排版提案（CC 回寫的頁面級提案 root，含【變更摘要】/【結構診斷】/【重排結果】）
   → Bear ✅ 套用 → extension 兩步（【重排結果】促升為正文、##/### 轉 heading → 刪舊正文與提案 root），套用前重驗歸零＋零位移
   → Bear ↩ 退回 → extension 刪整份提案（原稿本來就沒動過）
   2026-09-03 起套用**不留備份**（Bear 決定：零位移已逐字驗證，同頁存第二份是多此一舉）；↺ 還原／🧹 清備份只對更早留下的舊 🗄 備份有效
```

## 七、定稿轉 Hugo（三個歸零條件）

Bear 說「〈頁名〉轉 Hugo」時，CC 先檢查：**① 標記＝0 ② `#cc草稿`＝0**（未收編的 CC 草稿）**③ `#cc排版提案`＝0**（未處理的整篇重排版提案——見九；有提案代表版面還沒定，先讓 Bear 套用或退回）。
- 不滿足 → 列出擋下，不轉。
- **照片**：草稿裡 `![📷 …](https://composer.agoodbear.com/r/<refId>?s=…)` 是 Blog Composer 照片參照。成稿時抓出 refId → `POST localhost:8765/api/roam-ref-fetch` 換原檔 → 走 Hugo 媒材管線（詳見 blog-composer/ROAM-REFS.md）。那條 `/r/` URL 是縮圖不是原檔，別直接用。
- 滿足 → 讀整頁產 `content/posts/<type>-post-N.md`（沿用 ecg/study/travel/erlife-post-N 慣例；**Hugo 禁 H1**；排除「🗂 素材」子樹、**任何位置開頭為 🗂 的 block（含掛在稿底下的「🗂 改稿版次」，見§十）**與「🗄 …排版前備份／還原前狀態」`#cc排版備份` 子樹**；跑 zhtw-mcp lint）→ 部署 → 回 Roam 頁首寫一行 `✅ 已發佈 → <url> <日期>`，該頁視為封存（之後小修直接改 Hugo，大改才重開 Roam 頁）。
- 若有明示保留的 `#cc草稿` 段落 → 轉稿前必過 `/de-ai-zhtw`（只掃 CC 草稿出身的段落，Bear 原文絕不進）。

## 八、聲音守則（獨立成節，最高優先）

- **潤**：最小編輯距離、保留 Bear 用詞與判斷句、禁 AI 過場語與擴寫。
- **接**：先讀 `reference_ecg_writing_style_profile`；產出自知是草稿，不假裝成 Bear 的定稿。
- **查**：醫學數字兩路徑交叉、走 OpenEvidence；查不實寫「待確認」，禁編造引用/數字。**查證的【提案】整合版＝最小編輯把來源織進 Bear 原句（不重寫、不改語氣），寫不出/不宜就不給提案**。
- 一切以「這是 Bear 的文章、Bear 的聲音」為最高判準。CC 是紅筆，不是作者。

## 九、整篇重排版（📐 Reformat）

Bear 在 extension 右下點 **📐 重排版**（歸零才給打包）→ 複製一份「排版任務」貼給新開的 CC。這是**「排版」不是「改稿」**：CC 只出一份隔離的提案樹，extension 逐字機械驗證後、由 Bear 一鍵套用。

**觸發這件事的典型情境（v9 起寫進 prompt）**：來到這一步的稿子都是**多輪「請CC修改」之後**的成品——內容完整，但「接」的草稿是插在「當時標記掛在哪」而不是「內容該在哪」，「潤」只動被圈的那句、不管前後銜接。所以病灶固定是那幾樣：段落顆粒忽長忽短、同一主題散在相隔很遠的兩三處、某段讀起來像後來塞進去的、全篇平鋪沒骨架。前三樣屬「版面」，CC 動手；**「順序」CC 不准動手，但必須全部診斷出來**（見下鐵律 4 與【結構診斷】）。

**憲法豁免（唯一一處）**：`#cc排版提案` root 是 §一.3「不得在原稿區新增 block」的**唯一豁免**——它是**頁面級的標記 block**，跟 `#請cc修改` 同性質，CC 的字仍然關在標記裡、帶著身分、不匿名混入正文。除了這棵提案樹，原稿一個 block 都不准動（不 update、不 delete、不 move）。

**鐵律（違反任一條＝任務作廢）：**
1. Bear 的每一個字逐字保留：禁改字、禁換詞、禁增刪內容、禁修錯字、禁加過場句。extension 會逐字機械比對＋格式記號守恆計數，有位移＝整份提案被鎖死、套不了。
2. 只能做六件事：① 加標題（獨立 block、`## `章節／`### `小節）——**v11 起 CC 不准自己造標題**，只有兩種來源：**(a) 升格**「`## ((uid))`」把 Bear 自己寫的那一句變成標題（字不變、uid 不變、引用不斷，長度與數字都不限）；**(b) 路標白名單**（見下方「標題來源」）。**判準：Bear 折疊到只剩標題時，要能照著重講一次這篇**② 層級化：**(a) 章節縮排**（2026-09-07 Bear 拍板，v9 起預設）——每節正文縮排成該節 `## ` 標題的**子層**，`### ` 小節縮在所屬 `## ` 之下、其正文再縮一層；第一個標題之前的開場段落留頂層。目的是 Roam 折疊 bullet 就能收成全篇骨架，**這是「觀看體驗」的主力，別省**。**(b)** 連續平行短句縮排為子層 ③ 切分過長段落（只在原有標點處切，不增刪字元）④ 合併零碎段落（直接串接、不補字補標點；要補才通順→寫【結構診斷】）⑤ 整句加粗（只對「可直接抄進筆記的臨床結論／判斷整句」加 `**…**`，全篇新增 ≤5 處，每處列進【變更摘要】）⑥ 清雜訊（刪純空白 block）。Bear 既有的 `**`、`^^`、`[[]]`、`(())`、`{{}}`、圖片連結原樣保留、不增不減。
   - **縮排安全性（2026-09-07 實測 16 條，非推論）**：`gatherBodyBlocks`／提案側攤平都是 DFS，縮排只改層級不改 DFS 順序，逐字串接比對照過；`applyReformat` 促升的是【重排結果】的直接子層（＝各 `## ` 標題），子樹跟著 `moveBlock` 走，`headingUpdates` 是遞迴收集所以巢狀 `### ` 也轉得到。**純 prompt 改動，驗證器一行沒動。**
3. 不碰：「🗂 素材／背景」子樹、「🗄」備份子樹、「✅ 已發佈」行、所有 `#標記` block。照片 block（`![📷 …](composer.agoodbear.com/…)`）逐字保留、跟著原本相鄰段落放。
4. **v10 起可以搬移段落、跨節重排、合併**——這正是 v10 做出來的目的（v9 以前禁止，因為驗證是「全篇逐字串接比對」，搬一下就 `textOk:false`）。**但被其他 block 引用的段落是石頭**：只准整段搬，不准切、不准併掉、不准加粗、不准刪；石頭清單由 📐 打包時附在 prompt 裡。每一處搬移由 extension 從樹算出並列在卡片，不靠 CC 自報。順序仍是 Bear 的作者判斷，所以**你動手之前先在【結構診斷】把理由寫出來**。
5. 你的全部產出只放進下述提案樹。

**⚠️ 套用會炸掉 block ref——v9 的問題，v10 已根治（保留紀錄）**

> v10 起套用只搬不刪，uid 全部保住，以下三步 SOP 與 `fix_refs.py` 都不再需要。留著是因為它解釋了為什麼 v10 要這樣設計，也適用於任何「刪舊建新」的流程。

`applyReformat` 是「促升新 block → 刪舊 block」，舊 uid 全部消失。**Roam 在刪掉被引用的 block 時，會把指向它的 `((uid))` 就地展開成該 block 的整段文字**——不是顯示失效，是靜默變成靜態文字。零位移驗證只跑在套用「之前」，抓不到這件事；文字不會掉，但活的引用死了，而且 `[文字](((uid)))` 這種 markdown 連結會被撐破。2026-09-07 這頁實際炸掉 16 處、14 個來源 block、跨 6 個頁面（含「原稿的儲藏室」「Bear's medium」兩個索引頁）。

**SOP（CC 每次重排都要做，三步缺一不可）**：

1. **建提案「之前」**先盤點入向引用，存成 JSON（**srcStr 一定要一起存**，那是事後唯一的還原依據）：
   ```clojure
   [:find ?tgtUid ?srcUid ?srcTitle ?srcStr
    :where [?tgt :block/page ?pg] [?pg :block/uid "<pageUid>"] [?tgt :block/uid ?tgtUid]
           [?src :block/refs ?tgt] [?src :block/uid ?srcUid] [?src :block/string ?srcStr]
           [?src :block/page ?sp] [?sp :node/title ?srcTitle]]
   ```
2. **建提案時自行指定 uid**（`data.block.create` 吃 `block.uid`），產出「舊 uid → 新 uid」對照表存檔。沒有這張表就修不回來。
3. **Bear 套用「之後」立刻修**：拿步驟 1 的原文，把 `((舊))` 換成 `((新))` 寫回來源 block。修完回讀驗證，並確認頁面上每個 ref 都指得到人（`pull` 每個 uid 不得為 None）。
   - 覆蓋前要先確認「現況是不是只由原文展開而來」（把原文依 ref token 切開，每個字面片段須依序出現在現況裡）；不符合就跳過、不要硬蓋——那代表有人改過。
   - 腳本樣板：scratchpad `fix_refs.py`（含 `--dry-run`），配 `inbound_refs.json` + `uid_mapping.json`。

**還要在【結構診斷】裡寫明**：被指到幾個 block、幾處引用、哪些來自外頁，讓 Bear 按之前就知道會動到別頁。**不要只寫「引用會斷掉」——要寫「會被展開成文字」**，因為前者聽起來像壞掉很明顯，後者才是實情：它看起來沒事，要對帳才發現。

**提案樹格式**：在頁面**最底部**建 top-level block `#cc排版提案 【整篇重排版】YYYY-MM-DD`，其下三個子 block：
- `【變更摘要】升格標題 N｜新建路標 N｜切分 N｜合併 N｜加粗 N｜清空行 N`——子層逐條列明細（每個升格句的 uid＋全文、每個路標全文、每處合併/切分/加粗的位置與原文前 10 字）。
- `【結構診斷】離群 N｜接縫 N｜頭尾 <撐得住／要補>`（v9 起取代舊的 `【建議】`；extension 兩個欄名都認得）——**這一節不准寫「無」交差**，要逐段掃過才准下結論。四個必填子層：
  - `【骨架對位】`（**v11 新增，這一格是重點**）骨架 ①–⑪ 逐格一行：`② 病人一句話速寫｜((uid)) 或「本篇無」｜為什麼歸這格：<一句>`。歸不進任何一格的段落列在最後 `⑫ 歸不進去的`，一段一行寫理由。
  - `【節次地圖】` 每節一行：`## 標題｜第X–Y段｜這節在講：<一句話>`。
  - `【離群段】` 跟所在節主題不合、或跟同主題段落被隔很遠的段落，一段一行：`第X段〔原文前12字〕｜現在在<節>｜建議移到<節>之後｜理由：<一句>`。逐段檢查後真的沒有 → 寫「逐段檢查 N 段，無離群」（N 要寫實數）。
  - `【接縫】` 讀起來銜接生硬、像後來塞進去的段落（多輪改稿最常見的病灶），一處一行：斷在哪兩段之間、缺的是什麼（轉折？前提？跟前面重複了？）。**只診斷，不准補字。**
  - `【頭尾】` 開頭第一段、結尾最後一段各評一句：還撐不撐得住？撐不住是缺什麼？
- `【切分】`（沒有就不建）每行一條：`((uid))｜切點：「…前12字」‖「後12字…」→ ((新uid))`，或 `((uid))｜依換行 → ((新uid1)) ((新uid2)) …`
- `【合併】`（沒有就不建）每行一條：`((存活uid)) ＋ ((被併掉uid)) …｜接合：無／換行／空格`
- `【加粗】`（沒有就不建）每行一條：`((uid)) 「整句」`，全篇 ≤5
- `【重排結果】`——其**直接子層＝重排後的完整正文樹**。**v11 起每個節點只准是三種之一**：`((uid))`（正文 block 維持原樣）、`## ((uid))`／`### ((uid))`（**升格**：同一個 block 變成標題，字與 uid 都不動）、`## 路標`／`### 路標`（新建，**必須命中白名單**，全篇 ≤15 個）。**葉節點＝該段連同它現有的整棵子樹原樣照搬**；有子節點＝子層由你明列。所以只有動到結構的地方要展開。不要寫 order／index，順序就是樹本身。

**v10 的安全模型換了（2026-09-07）**：提案樹裡**沒有 Bear 的字**，只有 `((uid))`。所以「不改字」不是驗出來的、是建構出來的——CC 從頭到尾沒有複製過任何內文。驗證改成 13 條結構檢查（節點型別／標題限制／uid 存在／不重複／**覆蓋（漏段＝孤兒）**／不得引用排除區／石頭不可變／切分／合併／加粗／每 uid 最多一種文字操作），套用改成「只 `moveBlock` Bear 原本的 block、最後一步之前零 delete」。**block ref 不會再斷**（v9 那次一次炸掉 16 個引用、跨 6 個頁面），heading／折疊狀態／`:edit/time` 也一併保住。中斷後**再按一次套用就接著做完**（Phase 1 冪等）。

**extension 端（Bear 按的、CC 不做）**：零位移驗證（去 `**`/`__`、壓空白後逐字串接比對＋格式記號次數守恆；剔除提案側 `##`/`###` 標題 block）→ 驗過才解鎖 ✅ 套用 → 兩步（【重排結果】促升為正文、`##`/`###` 轉 Roam heading 屬性 → 刪舊正文、刪提案 root）。**不留備份**（2026-09-03 Bear 決定；驗證已保證逐字等值）。CC 只負責產出合格提案；套用是 Bear 在 extension 上的動作。

**回 chat 的對帳清單**：各章標題＋每類變更數，**外加【結構診斷】的離群段與接縫逐條列在 chat**（Bear 要直接讀，不想再翻回 Roam）。

**CC 產提案的實作提醒（2026-09-03 首跑經驗）**：不要手抄正文——用本機 Roam API（`data.pull`）拉整頁樹，以 uid 引用原字串做切分／加粗／縮排／加標題，先用從 extension.js 抽出的 `verifyZeroDrift` 驗，再用 `data.block.fromMarkdown("tmp")` 佔位＋`data.block.update` 寫 literal 字串（避開 markdown parse 改掉「1. 」「## 」），Bear 既有的 heading 用 update 的 `heading` 參數保留，建完重新 pull 回讀再驗一次。切分點兩側不能有空白（trim 會吃掉）；切分後的出處 child 掛在最後一段底下才不會打亂逐字串接順序。

### 標題來源三選一（v11，2026-09-07）

Bear：「你排的沒有我的靈魂。」實測他 18 篇 `ecg-post-*.md` 的 **241 個標題，108 個（44%）會被 v10 的護欄擋掉**（≤30 字、禁數字），被擋的正是 `**Step 1:排除artifact**`、`如果這時候的AIVR是115下，你還是認得出AIVR嗎?`、`這.....是不是VT` 這種**有靈魂的**；能通過的只剩 `基本知識`、`參考資料:` 這種死標籤。**他的標題是在說話（問句／懸念／驚嘆／整句臨床結論），不是目錄條目。**

護欄的原意（防 CC 把醫學數字寫進不進逐字比對的通道）是對的，但正解不是放寬長度，是**斷掉 CC 造標題的權利**：

| 來源 | 語法 | 限制 |
|---|---|---|
| **(a) 升格**（優先） | `## ((uid))`／`### ((uid))` | 無。字是 Bear 的、uid 不變、`((ref))` 不斷、`:edit/time` 不動 |
| **(b) 路標白名單** | `## Case繼續` 等 | 全篇 ≤15 個；必須命中白名單 |
| (c) 其他 | — | `V2_HEAD_FREE` 退件 |

白名單（取自 18 篇實測：拉回個案路標 27 次／10 篇、收尾固定節 38 次／17 篇）：
`Case繼續`／`Case個案繼續`／`個案繼續`／`Back to case`／`再度Back to Case`／`拉回正題`／`回到正題`／`回到此個案`／`回到個案`／`回到病人`／`回到 case`／`先問自己幾個問題`／`先來幾個問題`／`有幾個問題`／`我列出幾個好玩的問題`／`學習重點`／`文章重點`／`Learning Points`／`參考資料`／`參考文獻`／`事後回顧感想`／`延伸閱讀`／`註釋與出處`／`統整`／`小結`

比對時會正規化（拿掉 `**`、`<mark>`、前導 emoji、尾巴的 `~~`／冒號），但**建出來用你寫的原字串**，所以 `**❤️Case個案繼續**`、`<mark>Case繼續~~</mark>`、`↩️Back to case` 這些裝飾都留得住。夾帶私貨（`❤️Case個案繼續，病人後來死了`）會被擋。**長度上限與禁數字整組拿掉了**——那兩條只在「CC 自由造句」的世界裡才有意義。

驗證：39 條對抗測試（自線上 `extension.js` 抽出的碼，`extract_core.py`）＋真 Roam 端到端 11 條斷言＋冪等重跑（`scratchpad/e2e_v11.mjs`）。

### 排版依據＝Bear 的敘事骨架（v11，2026-09-07）

**排版順序不是 CC 的判斷，是骨架。** 動手前先讀 `~/Desktop/Claude Code專用檔/ecg-writing-style-profile.md` §1（Fable 5 逐篇精讀 37 篇提煉、~180 處原文佐證）：

① 邀請開場 → ② 病人一句話速寫 → ③ 第一張 ECG＋五宮格判讀 → ④ 先問自己幾個問題 Q1/Q2/Q3（隱形鋼骨）→ **(⑤ 教學離題 ↔ ⑥ 拉回個案) × n 輪**（像剝洋蔥，post-16 有四次）→ ⑦ 反轉／serial ECG → ⑧ 診斷揭曉（CAG）→ ⑨ 感悟／後怕 → ⑩ 學習重點 → ⑪ 參考資料

⑤⑥ 每交替一輪就用一個 (b) 路標把讀者接回病人身上——那是他的節拍器。每段歸哪一格要寫進 `【骨架對位】` 並交代理由，Bear 要能逐條檢查。缺的格子寫「本篇無」，**不要自己生內容去填**。

**為什麼這條規則存在**：事後重排排不出靈魂——那是寫的時候就決定的。骨架早就在風格檔裡（含 11 格與原文佐證），但一直只被當「寫作風格」用、沒接進排版流程；Bear 自己寫的「自行排版大綱」與這 11 格 1:1 對得上，他不是在改版面，是在把 CC 漏掉的骨架補回去。**寫 ECG 原稿時第一個動作就該是把骨架建好，往格子裡填**，這樣「事後重排」根本不會發生。

### ⚠️ 走過 `/ecg-case` 的稿子：順序已定案，重排版只做版面（v13，2026-09-08）

**怎麼辨識**：頁面的「🗂 素材工作區」底下有 **`路線圖`** block（Q1–Qn 表格，含「錨點／誰的題／回 case？」欄）與 **`擺盪表`**。
📐 打包時 extension 會偵測到並在任務開頭插一行警語（見 extension.js `buildReformatPrompt`）。

**規則**：

1. **順序不准動。** 這份稿的骨架是 `/ecg-case` 在動筆**之前**跟 Bear 逐輪討論出來的——
   路線圖 Q1–Qn 就是骨架 ④、擺盪表就是 (⑤↔⑥)×n。**格子在寫之前就填好了，不是事後排出來的。**
   ➜ `【離群段】` 一律寫「本篇順序由 /ecg-case 定案，不提搬移建議」，**不要**把討論出來的轉折當離群段。
2. **`【骨架對位】` 對照那份路線圖，不是自己重新歸格。** 每個 Q 對到骨架哪一格、擺盪點在哪，
   路線圖上已經寫好了，照抄對位即可。**歸不進 ①–⑪ 的段落（例如診斷停滯表、UDMI 命名段）
   寫進 ⑫ 並註明「/ecg-case 主軸產物，非離群」——不要硬塞進格子，也不要建議刪。**
3. **你只做版面**：章節縮排（折疊能看骨架，這項最有價值、別省）、切分過長段落、合併零碎段落、
   整句加粗 ≤5、清空白行。**標題一律走升格（`## ((uid))`），不新建路標**——路線圖的 Q 標題
   本來就是 Bear 定的，升格就好。
4. **仍然要做的診斷**：`【接縫】` 與 `【頭尾】` 照常逐段檢查並回報——多輪「請CC修改」之後
   銜接生硬的問題，跟骨架對不對是兩件事，這部分 `/ecg-case` 不保證。

**為什麼要有這條**：重排版是**開新 session** 做的，那個 CC 只拿到打包文字，**它不知道這份稿的骨架
是討論出來的**，會照 ①–⑪ 硬套，把 Bear 逐輪談定的轉折當成「離群段」建議搬走。
2026-09-08 Bear 問「重排版是不是會把骨架重整？」——問題本身就是警訊：
**一個會讓作者擔心自己的結構被推翻的工具，預設行為就是錯的。**

---

## 十、改稿版次記錄（🗂 版次 block，2026-09-07 Bear 要求）

**問題**：一頁原稿經過 v1／v2、再經過好幾輪「請CC修改」和一次整篇重排版之後，打開頁面看不出「現在是第幾版、上一輪什麼時候改的、這一輪改了什麼」。

**規則（CC 每輪都要做，不必等 Bear 開口）**：

- **位置（2026-09-07 Bear 訂正）**：掛在**它所描述的那份稿底下、當第一個子 block**（例如 v2 稿 block 的 order 0），**不是頁面 top-level**。理由：一頁常常同時有 v1／v2／素材，版次表掛在頁面層根本分不清在講哪一版；它屬於那份稿，就該跟著那份稿走。稿版換新（v2→v3）時，在新稿底下另開一份，舊的留在舊稿底下不動。
- **開頭一定要是 `🗂`**，方便任何位置的機械過濾都抓得到。
- ⚠️ **但要清楚一件事：`isReformatExcludedRoot()` 只對 top-level root 生效**。版次表既然掛在稿底下（非 top-level），它**不會**被自動排除，會被 `gatherBodyBlocks` 當成正文的一部分。因此：
  1. **整篇重排版**：把版次表當正文逐字帶進【重排結果】（原樣保留、不切不動不加粗）。
  2. ~~更新版次表必須在 pull 整頁建提案之前~~／~~有 pending 提案時不要更新~~ —— **v10 起這兩條作廢**：`gatherBodyStruct` 把任意深度的 🗂／🗄 當 atom，不進正文、提案不准引用、跟著父層走，套用後自然落在該 parent 最前面（正好是 order 0）。版次表隨時可以更新。
  4. **§七 轉 Hugo**：CC 要自己排除**任何位置**開頭是 `🗂` 的 block，不是只看 top-level。
- **格式**：parent 一行＝現況，children 一輪一行、**最新在最上面**。

```
🗂 改稿版次｜v<稿版> · 第 <N> 輪 · <YYYY-MM-DD HH:MM>（<這一輪是什麼>，<狀態>）
  - v2 · 第 3 輪｜2026-09-07 16:43｜整篇重排版提案（標題 +7／縮排 7 節／切分 5／加粗 2／清空行 3；結構診斷：離群 6／接縫 7）→ ⏳ 待套用
  - v2 · 第 2 輪｜2026-09-07 15:57–15:58｜請CC修改 2 處（Nazir 2017 原文逐字貼上、心臟受累→心臟受傷）→ ✅ 已套用
  - v2 · 第 1 輪｜2026-09-07 15:22–15:57｜請CC修改 19 處（Lake Louise 2018 全條目、Thomas 29 例只有 7 例做過 CAG、四份指引的 aVR 條文、天平圖、PubMed 截圖）→ ✅ 已套用
  - v2 · 第 0 版｜2026-09-07 10:35｜Opus 5 把 v1 學術 case report 重寫成部落格 ECG 教學文體
```

- **編號**：`v<稿版>` ＝內容版本（v1 學術版、v2 部落格版…，跟頁上「📝 …初稿（vN）」對齊）；`第 <N> 輪` ＝該稿版底下的改稿輪次，從 1 起算，稿版換新時歸零（寫成「第 0 版」那一行）。
- **時間**：Asia/Taipei，`YYYY-MM-DD HH:MM`。**不准憑印象填**——用 `:edit/time` 撈真實時間（`[:find ?u ?e :in $ [?u ...] :where [?b :block/uid ?u] [?b :edit/time ?e]]`），一輪跨多個時間就寫區間。潤稿落地的時間＝Bear 按 ✅ 的時間，不是 CC 回寫提案的時間，兩者要分清楚。
- **一行要交代三件事**：做了什麼（幾處／哪幾類）、重點內容（挑最有份量的兩三項，不要流水帳）、**狀態**（`✅ 已套用`／`⏳ 待套用`／`↩ 已退回`）。
- **⚠️ 整篇重排版那一輪，v10.1 起由 extension 自動寫（2026-09-07）**：`applyReformatPlan` 稽核通過後呼叫 `bumpVersionLog()`，自己找到本頁的 `🗂 改稿版次` block、解析出版號與輪次、+1 之後補一行（含機器算出來的搬移／切分／合併／加粗／清空行數字與稽核三項）。找不到就在頁面最上面新建一個並在 toast 提醒你搬到該稿底下；找到超過一個就不動、只在 Console 警告。**CC 不要再手動補這一輪**，會變兩行。
- **其餘輪次（請CC修改）仍由 CC 手寫**，觸發點如下。理由寫在這裡免得又忘：2026-09-07 同一天 CC 漏了兩次（建提案時該記「待套用」、套用後該改「已套用」），兩次都要 Bear 提醒才補——**規則只約束「有沒有想起來」，所以能給機器做的就別留給紀律**。
- **何時寫（兩個觸發點，兩個都不准漏；2026-09-07 Bear 再次交代）**：
  1. **一輪「請CC修改」收斂**——該輪所有標記都回寫完、Bear 也審完（標記歸零），就記一輪。**在 chat 送出對帳清單之前先更新**，不要事後補。
  2. **整篇重排版**——分兩次動筆：提案樹建好時先記一行、狀態寫 `⏳ 待套用`；**Bear 按下套用之後回來把狀態改成 `✅ 已套用`**。套用如果衍生後續處理（例如 2026-09-07 那次的 16 個 block ref 修復），那是**另記一輪**，不要塞回同一行。
  同一輪不要開兩行；不同輪不要併成一行。
- **Bear 退回重改**：不新增一輪，把該輪那行的狀態改成 `↩ 已退回`，等重送後再改回 `⏳ 待套用`。

**與其他流程的關係**：轉 Hugo 時這個 block 跟 🗂 素材、🗄 備份一樣排除，不進成稿；整篇重排版時視同 `#標記` block，不碰、不搬、不進【重排結果】。
